import { Text} from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Chat = () =>{
    return (
        <SafeAreaView>
            <Text>Chat d</Text>
        </SafeAreaView>
    )
}

export default Chat;